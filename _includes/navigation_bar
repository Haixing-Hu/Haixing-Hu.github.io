{% comment %}<!--
The `navigation_bar` generate the navigation bar of the site.

Usage:
  1. include `navigation_bar`

Example:

  <!DOCTYPE html>
  <html>
    <head>
      {% include html_meta %}
      ...
      {% include mathjax_support %}
    </head>
    <body>
      {% include navigation_bar %}
      ...
    </body>
  </html>

-->{% endcomment %}

<div class="navbar">
  <div class="navbar-inner">
    <div class="container-narrow">
      <a class="brand" href="{{ HOME_PATH }}">{{ site.author.name }}</a>
      <ul class="nav pull-right">
        {% for sec in site.sections %}
          {% capture sec_url %}/{{sec}}.html{% endcapture %}
          {% if page.url == sec_url or page.category == sec %}
          <li class="active">
            <a href="{{ BASE_PATH }}{{sec_url}}" class="active">{{sec | capitalize}}</a>
          </li>
          {% else %}
      	  <li>
            <a href="{{ BASE_PATH }}{{sec_url}}">{{sec | capitalize}}</a>
          </li>
      	  {% endif %}
        {% endfor %}
        {% if site.JB.rss_path %}
        <li>
          <a href="{{ site.JB.rss_path }}"
             rel="subscribe-rss"
             title="subscribe via RSS feed">
              <img src="/img/rss.png" alt="rss"/>
          </a>
        </li>
        {% endif %}
      </ul>
    </div>
  </div>
</div>
